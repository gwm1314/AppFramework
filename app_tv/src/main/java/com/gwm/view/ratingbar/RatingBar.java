package com.gwm.view.ratingbar;

import android.content.Context;
import android.util.AttributeSet;

/**
 *
 */
public class RatingBar extends android.widget.RatingBar{
    public RatingBar(Context context){
        super(context);
    }
    public RatingBar(Context context,AttributeSet attr){
        super(context);
    }
    public RatingBar(Context context,AttributeSet attr,int style){
        super(context);
    }
}
